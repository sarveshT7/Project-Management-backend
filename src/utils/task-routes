// // src/routes/task.routes.ts
// import { Router } from 'express';
// import {
//   createTask,
//   getAllTasks,
//   getTaskById,
//   updateTask,
//   deleteTask,
//   getTasksByProject,
//   updateTaskStatus,
//   addComment
// } from '../controllers/task.controller';
// import { authenticate } from '../middleware/auth';
// import { validate, validateParams, validateQuery, schemas, paramSchemas, querySchemas } from '../middleware/validation';

// const router = Router();

// // All routes require authentication
// router.use(authenticate);

// // Task CRUD routes
// router.post('/', validate(schemas.createTask), createTask);
// router.get('/', validateQuery(querySchemas.tasksQuery), getAllTasks);
// router.get('/:id', validateParams(paramSchemas.objectId), getTaskById);
// router.put('/:id', validateParams(paramSchemas.objectId), validate(schemas.updateTask), updateTask);
// router.delete('/:id', validateParams(paramSchemas.objectId), deleteTask);

// // Task status management
// router.patch('/:id/status', validateParams(paramSchemas.objectId), validate(schemas.updateTaskStatus), updateTaskStatus);

// // Task comments
// router.post('/:id/comments', validateParams(paramSchemas.objectId), validate(schemas.addComment), addComment);

// // Get tasks by project
// router.get('/project/:projectId', validateParams(paramSchemas.projectId), getTasksByProject);

// export default router;